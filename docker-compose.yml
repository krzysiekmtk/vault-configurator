services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: vault-configurator
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
