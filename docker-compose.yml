services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: vault-configurator
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
